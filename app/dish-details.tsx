import React from "react";
import { Image, View } from "react-native";

const DishDetails = () => {
  return (
    <View>
      <Image height={300} source={{ uri: "image" }} />
      <View></View>
    </View>
  );
};

export default DishDetails;
